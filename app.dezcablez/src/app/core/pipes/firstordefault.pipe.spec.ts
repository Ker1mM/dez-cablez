import { FirstordefaultPipe } from './firstordefault.pipe';

describe('FirstordefaultPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstordefaultPipe();
    expect(pipe).toBeTruthy();
  });
});
